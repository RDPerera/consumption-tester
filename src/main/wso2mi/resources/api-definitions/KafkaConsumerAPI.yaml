openapi: 3.0.1
info:
  title: KafkaConsumerAPI
  description: API Definition of KafkaConsumerAPI
  version: 1.0.0
servers:
- url: http://localhost:8290/kafkaConsumer
- url: https://localhost:8253/kafkaConsumer
paths:
  /:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
